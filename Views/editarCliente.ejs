<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar Cliente - GYMCODE</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <main class="container mx-auto mt-8 p-4">
        <section class="bg-indigo-100 rounded-lg shadow-md p-6 mb-6">
            <h2 class="text-3xl font-semibold text-indigo-700 mb-4">Editar Cliente</h2>
            <form action="/editar/<%= client.id %>" method="POST" class="space-y-4">
                <div>
                    <label for="nombre" class="block text-gray-700 text-sm font-bold mb-2">Nombre Completo:</label>
                    <input type="text" id="nombre" name="nombre" value="<%= client.nombre %>" required
                        class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700">
                </div>
                <div>
                    <label for="email" class="block text-gray-700 text-sm font-bold mb-2">Email:</label>
                    <input type="email" id="email" name="email" value="<%= client.email %>" required
                        class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700">
                </div>
                <div>
                    <label for="telefono" class="block text-gray-700 text-sm font-bold mb-2">Teléfono:</label>
                    <input type="text" id="telefono" name="telefono" value="<%= client.telefono %>" required
                        class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700">
                </div>
                <div>
                    <label for="membresia" class="block text-gray-700 text-sm font-bold mb-2">Estado de Membresía:</label>
                    <select id="membresia" name="membresia" class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700">
                        <option value="Activa" <%= client.membresia === 'Activa' ? 'selected' : '' %>>Activa</option>
                        <option value="Inactiva" <%= client.membresia === 'Inactiva' ? 'selected' : '' %>>Inactiva</option>
                    </select>
                </div>
                <button type="submit" class="bg-yellow-500 hover:bg-yellow-700 text-white font-bold py-2 px-4 rounded-full">Guardar Cambios</button>
            </form>
            <a href="/index" class="inline-block mt-4 bg-indigo-500 hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded-full">Volver</a>
        </section>
    </main>
</body>
</html>